package org.example.constants;

public interface ValidationErrorConstants {
    String EMAIL_NOT_UNIQUE = "Account with this email already exists ";
    String PHONE_NOT_UNIQUE = "Account with this phone number already exists";
}
